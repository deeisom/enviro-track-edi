
The user wants to bulk-archive all legacy projects whose project_number starts with "PR-24" or earlier (i.e. pre-EnviroTrack-format numbers from 2024 and before) to status `6.0` (Closed / Complete).

Let me check what's actually in the DB to confirm scope before acting.
